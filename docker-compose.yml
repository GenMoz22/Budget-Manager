services:
  web:
    image: genmoz22/budgetmanager:latest
    container_name: budget_manager_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - budget_db_data:/app/instance
    restart: unless-stopped

volumes:
  budget_db_data: